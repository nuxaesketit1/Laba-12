#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int Sign(double Y) {
	if (Y < 0) return -1;
	else if (Y == 0) return 0;
	else return 1;
};

int main() {

	setlocale(0, "");

	double a, b;

	cout << "Введите два вещественных числа A и B : " << endl;
	cin >> a >> b;

	cout << "Сумма значений вещественных чисел " << a << " и " << b << " равна " << Sign(a) + Sign(b) << ". " << endl;

	cout << endl;

	system("pause");

	return 0;

}
