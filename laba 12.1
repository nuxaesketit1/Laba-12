#include <iostream>

#include <cmath>

#include <iomanip>

using namespace std;

double PowerA3(double A) {
	return pow(A, 3);
}

int main() {

	setlocale(0, "");

	double a, b = 1;

	for (int i = 0; i < 5; i++) {

		cout << "Введите число A : ";

		cin >> a;

		cout << a << "^3 = " << PowerA3(a) << endl;

	}
	
	cout << endl;

	system("pause");

	return 0;

}
